# Cómo publicar tu página web en Firebase Hosting

Esta guía asume que ya tienes:

- Un proyecto React (Vite) funcionando en `npm run dev`.
- Firebase configurado en `src/firebaseConfig.js`.
- CRUD apuntando a Firestore (sin json-server).

---

## 1. Verificar que el proyecto funciona en local

En la raíz del proyecto (donde está `package.json`):